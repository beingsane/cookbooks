# Shell


Set default shell

```
which bash
sudo chsh -s /bin/bash [username]
```

Show installed shell's:

```bash
cat /etc/shells
```

Show current shell:

```bash
echo $SHELL
```

Change the default shell:

```bash
chsh -s /bin/bash
```
